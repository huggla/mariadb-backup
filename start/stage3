# Set in stage2:
# -------------------------------------------
# set -e +a +m +s +i -f
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ "$isFirstRun" == "true" ]
then
   pwFile="$(makePwFileForUser $VAR_LINUX_USER)"
   eval "\$VAR_password_file_$VAR_LINUX_USER=$pwFile"
   writeCrontab
fi
execCmd "$VAR_FINAL_COMMAND"
