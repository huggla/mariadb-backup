# Set in stage2:
# -------------------------------------------
# set -e +a +m +s +i -f
# isFirstRun
# VAR_*
# All functions in /start/functions

pwFile="$(makePwFileForUser $VAR_LINUX_USER)"
if [ "$isFirstRun" == "true" ]
then
   writeCrontab
fi
execCmd "$VAR_FINAL_COMMAND"
